# Python virtual environment
.venv/
venv/

# Python
__pycache__/
*.pyc
*.pyo

# Node (if any)
node_modules/

# React 前端构建产物
packages/web/dist/
*.tsbuildinfo
.vite/
docs/developLog.md

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# Runtime data
data/

# 测试输出转储（本地运行产物）
tests/out.json

# assets 合并暂存（手工归并占位）
_assets_merge_hold/

# Personal notes
privatenote.md
devNote.md
private-manifest.json

# Agent instructions (local only)
testoutcome.md
# spike residues (may be locked by running processes)
spike-node/
spike-python/
.codebuddy/
.privart/
eprompt.md
/.playwright-cli/
# config
config.json
.mcp.json
# opencode adapter 运行时写入的项目级 MCP 配置（spawn opencode 会话时生成）
opencode.json
packages/qq/.env

# Runtime memory DB (per-character sqlite)
memory/char_*.sqlite
memory/char_*.sqlite-shm
memory/char_*.sqlite-wal

# e2e 测试临时产物（本地调试用，不入库）
_e2e_tmp/
packages/web/test-results/
packages/web/e2e/test-results/
_pan_test.log
_pan_codex_e2e*.log

# 本机 codex 配置目录（含本地 config.toml / 凭证）
.codex/

# Secrets / 本机绑定信息防误提交（注意：scripts/kimi-mcp-probe/*.log 历史上已被追踪，
.private
# gitignore 不影响已追踪文件，仅拦截新增）
auth_token*
*.env
*.log

# pytest temporary bases used by local adapter/worker regression runs
.pytest-tmp*/

# Local Playwright browser binaries used by isolated E2E validation
.playwright-browsers/

# README screenshots: keep only the three published images below
/assets/*
!/assets/创建SMA第一步（点击此处，新建带参数的session）.png
!/assets/创建SMA第二步，选中SMA(注意务必选择noadapter的以便自行配置adapter).png
!/assets/树状的任务？树状来管理.png
!/assets/1.png
!/assets/2.png
!/assets/3.png
!/assets/4.png
!/assets/5.png
