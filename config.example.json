{
  "_如何使用": "复制本文件为 config.json，删除 _如何使用 这个键，修改剩余字段后重启服务。所有字段可选，省略时使用下方默认值。Pan 解释器使用顶层 python：config.json python > PAN_PYTHON > 当前解释器；普通路径写字符串，需要 py -3 等 launcher 参数时写 {command, args} 对象。",

  "port": 8768,

  "python": "",

  "startup": {
    "_字段说明": {
      "console_hidden": {
        "默认值": false,
        "可选值": [true, false],
        "说明": "scripts/start_pan.bat 启动时是否隐藏 Pan 控制台；false 显示独立控制台。仅影响脚本启动，直接运行 python main.py 仍为前台模式"
      }
    },
    "console_hidden": false
  },

  "cbc": {
    "_字段说明": {
      "model": {
        "默认值": "deepseek-v4-flash",
        "可选值": [
          "deepseek-v4-flash", "deepseek-v4-pro", "deepseek-v3-2-volc",
          "custom-local:deepseek-v4-pro",
          "glm-5.2", "glm-5.1", "glm-5.0", "glm-5.0-turbo", "glm-5v-turbo", "glm-4.7",
          "minimax-m3-pay", "minimax-m2.7",
          "kimi-k2.7", "kimi-k2.6",
          "hy3","所有可用模型"
        ],
        "说明":"cbc使用的默认模型"
      },
      "permission_mode": {
        "默认值": "bypassPermissions",
        "可选值": ["", "default", "acceptEdits", "bypassPermissions", "plan", "dontAsk", "auto"]
      },
      "always_thinking_enabled": {
        "默认值": false,
        "说明": "cbc 的 alwaysThinkingEnabled 设置；false 时 --effort 不生效"
      },
      "effort": {
        "默认值": "",
        "可选值": ["", "none", "off", "auto", "low", "medium", "high", "xhigh", "max", "ultracode"],
        "说明": "仅 always_thinking_enabled=true 时生效；空值表示不传递 --effort 参数"
      },
      "models": {
        "默认值": [],
        "说明": "建议不填写，将自动识别可用模型（cbc --help 解析）；填写代表限制可用模型。例: [\"my-model-1\", \"my-model-2\"]"
      }
    },
    "model": "deepseek-v4-flash",
    "permission_mode": "bypassPermissions",
    "always_thinking_enabled": false,
    "effort": "",
    "models": []
  },

  "claude": {
    "_字段说明": {
      "model": {
        "默认值": "",
        "说明": "空值表示使用 Claude Code 自己的默认模型；也可填写 claude 的完整模型名或 opus/sonnet/haiku 别名"
      },
      "models": {
        "默认值": [],
        "说明": "Claude CLI 没有稳定的模型清单接口；空值使用 Pan 内置别名，填写后限制前端模型选择"
      },
      "permission_mode": {
        "默认值": "bypassPermissions",
        "可选值": ["", "default", "acceptEdits", "bypassPermissions", "plan", "auto", "dontAsk", "manual"]
      },
      "effort": {
        "默认值": "",
        "可选值": ["", "low", "medium", "high", "xhigh", "max"]
      },
      "permission_prompt_tool": {
        "默认值": "mcp__pan__permission_prompt",
        "说明": "非 bypassPermissions 模式下接收 Claude 权限请求的 MCP 工具；只有挂载 pan MCP server 时才自动传入 CLI"
      },
      "permission_prompt_tool_supported": {
        "默认值": null,
        "说明": "null 自动检查 claude --help；某些 vendor build 可显式设 true/false"
      }
    },
    "model": "",
    "models": [],
    "permission_mode": "bypassPermissions",
    "effort": "",
    "permission_prompt_tool": "mcp__pan__permission_prompt",
    "permission_prompt_tool_supported": null
  },

  "kimi": {
    "_字段说明": {
      "model": {
        "默认值": "moonshot-cn/kimi-k2.6",
        "可选值": [
          "moonshot-cn/kimi-k2.6",
          "moonshot-cn/kimi-k2.7-code"
        ]
      },
      "permission_mode": {
        "默认值": "",
        "说明": "Kimi 的 -p/--prompt 模式暂不支持 -y/--auto/--plan，保持空值即可"
      },
      "always_thinking_enabled": {
        "默认值": false,
        "说明": "Kimi 的思考模式由 config.toml 控制，Pan 仅作记录"
      },
      "effort": {
        "默认值": "",
        "说明": "Kimi prompt 模式暂不支持 effort 命令行参数"
      },
      "models": {
        "默认值": [],
        "说明": "建议不填写，将自动识别可用模型（kimi config.toml 解析）；填写代表限制可用模型。例: [\"my-model-1\", \"my-model-2\"]"
      }
    },
    "model": "moonshot-cn/kimi-k2.6",
    "models": [],
    "permission_mode": "",
    "always_thinking_enabled": false,
    "effort": ""
  },

  "cbc_import": {
    "_字段说明": {
      "min_message_count": "最少消息数，低于此值不在导入列表显示（默认 5）",
      "max_sessions_shown": "导入列表最多显示数量（默认 30）",
      "exclude_workdir_patterns": "排除 workdir 含指定子串的 session，project_dir_exact_match=false 时生效",
      "project_dir_exact_match": "仅显示与当前项目路径完全匹配的 session",
      "import_recent_days": "只显示最近N天修改过的session（默认 30，0=不限）",
      "min_resume_bytes": "JSONL文件至少多大才算可resume（默认 200）"
    },
    "min_message_count": 5,
    "max_sessions_shown": 30,
    "exclude_workdir_patterns": [],
    "project_dir_exact_match": false,
    "import_recent_days": 30,
    "min_resume_bytes": 200
  },

  "remote": {
    "_字段说明": {
      "enabled": "是否启用远程通道（默认 false）。false 或缺失时 start_pan.bat 跳过远程通道，Pan Core 照常启动",
      "provider": "远程通道供应商，目前仅支持 cloudflare",
      "quick_tunnel": "start_pan.bat 按此字段分支（缺省视为 true，与 main.py 一致）。true = Quick Tunnel：运行 cloudflared tunnel --url http://127.0.0.1:<port>，无需 config.yml，Cloudflare 分配临时随机的 trycloudflare.com URL（每次启动/重启都会变化，仅适合开发测试，不宜作为长期稳定入口）；URL 会尝试打印在启动输出中，并写入 data/logs/pan_cf_quick_<port>.log。false = named tunnel：走 scripts/start_cf.ps1 既有路径（支持 PAN_CF_CONFIG 环境变量与 USERPROFILE 默认 yml 兜底、protocol 注入）",
      "config_path": "named tunnel 的 cloudflared config.yml 路径；仅 quick_tunnel=false 时必填（quick_tunnel=true 时忽略）。缺失或无效时隧道启动失败，start_pan.bat 仅提示 warning，Pan Core 不受影响",
      "binary_path": "cloudflared 可执行文件路径，留空则从 PATH 查找",
      "protocol": "named tunnel 的传输协议 auto/quic/http2（默认 http2）；仅 quick_tunnel=false 时由 start_cf.ps1 注入临时 yml",
      "status_port": "Remote 状态 HTTP 服务端口（默认 8769）"
    },
    "enabled": false,
    "provider": "cloudflare",
    "quick_tunnel": true,
    "config_path": "",
    "binary_path": "",
    "protocol": "http2",
    "status_port": 8769
  },

  "qq": {
    "_字段说明": {
      "enabled": "是否启动 QQ 模块（packages/qq/bot.py，NoneBot）：false 时 main.py 不启动 QQ bot（默认 true）。",
      "mode": "QQ 桥接模式（packages/qq/plugin.py 读取）：mirror=收到消息自动建 session 并回复（默认）；selective=消息只进 inbox+history 不自动回复，由 meta-agent 经 MCP（qq_read_inbox/qq_send_message）选择性处理。也可用环境变量 PAN_QQ_MODE 覆盖。",
      "python": "QQ bot 独立解释器路径（单一事实源，NoneBot 装在这里而非项目 .venv）：空 = 走默认解析链（PAN_QQ_PYTHON 环境变量 > 此处 > 平台默认 E:\\software\\miniforge\\python.exe）。setup.bat 探测到解释器后会自动写入。"
    },
    "enabled": true,
    "mode": "mirror",
    "python": ""
  },

  "worker": {
    "_字段说明": {
      "timeout_sec": "静默超时（秒）：running/queued 状态下持续无 stdout 输出超过该值时 kill（默认 300）",
      "idle_sec": "空闲回收（秒）：idle 状态（任务完成）持续超过该值时回收进程。held/zombie 跳过（默认 300）"
    },
    "timeout_sec": 300,
    "idle_sec": 300
  },

  "mcp": {
    "_字段说明": {
      "enabled_default": "已废弃（MCP 启用单一事实源收敛后不再读取）：MCP 启用由 session 的 mcp_servers 非空决定（非空即启用），本键保留仅作历史兼容记录，改它无效果"
    },
    "enabled_default": true
  },

  "logging": {
    "_字段说明": {
      "level": "日志级别：DEBUG/INFO/WARNING/ERROR（默认 INFO）",
      "file": "日志文件路径；相对路径以仓库根为基准（默认 data/logs/pan.log）",
      "max_bytes": "单个日志文件大小上限（字节），超过即轮转（默认 10MB）",
      "backup_count": "保留的轮转日志份数（大小轮转 .1/.2/... 与按天轮转 .YYYYMMDD 各自保留，默认 7）",
      "console": "是否同时在控制台输出（默认 true）"
    },
    "level": "INFO",
    "file": "data/logs/pan.log",
    "max_bytes": 10485760,
    "backup_count": 7,
    "console": true
  },

  "plugin_manifests": {
    "_字段说明": {
      "plugin_manifests": "manifest.json 文件或目录路径列表；默认包含项目模板与 Pan MCP，可追加外部/private manifest"
    }
  },
  "plugin_manifests": ["manifest.json", "packages/mcp/manifest.json"]
}
