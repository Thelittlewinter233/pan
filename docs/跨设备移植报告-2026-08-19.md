# Pan 跨设备（Windows）移植报告

日期：2026-08-19（2026-08-27 复核更新：CWD 残留现状、claude/codex 用户目录依赖；本机绝对路径已通用化处理）
范围：<仓库根> 全仓（packages/、scripts/、tests/、根配置文件、data/）
目标：在另一台 Windows 机器上运行同一项目（含整体拷贝目录 与 git clone 两种迁移方式）。
> 注：本报告写作时引用了 config.json / .mcp.json 的具体内容作为审计证据。这些文件是 gitignored 的本机配置，下文已将用户名 / 机器路径通用化，以 `<用户名>`、`<仓库根>` 等占位。

---

## 一、结论摘要

Pan **自身运行数据**（会话、角色、日志、workdirs、PID）全部相对仓库根解析，整体拷贝即可迁移。
真正阻碍移植的是三类内容：

1. **机器专属绝对路径 + 用户名**：config.json、.mcp.json、data/mcp-configs/ 运行时产物里写死了 `D:/project/...`、`C:\Users\<用户名>\...`；
2. **仓库外兄弟项目引用**：依赖 `<仓库外>/ai_coc`（插件 manifest + MCP rulewhisper）；
3. **用户目录 transcript 依赖**：cbc/kimi 的历史会话存在 `~/.codebuddy/projects` 与 `~/.kimi-code`，目录名按"绝对路径脱敏"生成，换路径后旧会话全部失联。

另有若干**端口默认值硬编码**与 **CWD 依赖**，属于次要问题。

### 两种迁移方式的关键差异

| 迁移方式 | 硬编码影响 |
|---|---|
| **整体拷贝目录** | config.json / .mcp.json / data/mcp-configs 里的绝对路径直接失效，必须改 |
| **git clone**（剔除 .gitignore 内容） | 这些文件本就不在版本库、不会带过去，**无需清理**；但残留的是**代码级**硬编码（见 §二·改） |

> 走 git clone 时，config.json / .mcp.json / data/ / .venv 都不会来，新机需按 config.example.json 重建 config.json、重建 .mcp.json、创建 venv。

---

## 二、硬编码清单（按严重度）

### A. 机器专属绝对路径 / 用户名 / 仓库外引用（换机必坏）

| 位置 | 内容 | 说明 |
|---|---|---|
| `config.json:50` | `"config_path": "C:\\Users\\<用户名>\\.cloudflared\\config.yml"` | cloudflared 隧道配置，**含用户名**。该 yml 是 named tunnel 的 ingress.hostname 与凭据来源，换机必须重配 |
| `config.json:64` | `"../ai_coc/pan_plugin/manifest.json"` | 仓库外兄弟项目引用，依赖 `ai_coc` 与 Pan 同级存在 |
| `.mcp.json:5` | `"command": "<仓库外>/ai_coc/.venv/Scripts/python"` | rulewhisper MCP 指向仓库外 venv |
| `.mcp.json:10` | `"cwd": "<仓库外>/ai_coc"` | rulewhisper MCP 工作目录 |
| `.mcp.json:14` | `"command": "<仓库根>/.venv/Scripts/python"` | pan MCP 的 venv 绝对路径 |
| `.mcp.json:19` | `"cwd": "<仓库根>"` | pan MCP 工作目录 |
| `data/mcp-configs/ses_*.mcp.json`（4 个运行时产物） | `"command": "D:\\project\\Pan\\.venv\\Scripts\\python"`、`"cwd": "D:\\project\\Pan"` | 会话级 MCP 配置，**运行时生成**，换路径后已建会话的 MCP 失效 |
| `scripts/start_cf.ps1:36` | `$cfConfig = "$env:USERPROFILE\\.cloudflared\\config-test.yml"` | 兜底路径（`PAN_CF_CONFIG` 可覆盖，属可接受但有环境依赖） |
| `tests/test_kimi_adapter.py:19` | `KIMI_TEST_WORKDIR = "C:/Users/<用户名>/AppData/Local/Temp/kimi-test"` | 测试夹具含用户名 |
| `scripts/repro_mcp_override.py:113/116/119` | `D:\project\Pan` | 调试脚本，非运行路径 |

> 注：config.json、.mcp.json、data/ 均在 .gitignore 中。若走 **git clone** 迁移，这些文件本就不会带过去（需在目标机重配）；若**整体拷贝目录**迁移，上述绝对路径会直接失效。

---

## 二·改、git clone 迁移视角：剔除 .gitignore 内容后仍存在的硬编码

本节只列 **git 版本库内被跟踪的文件**（`git grep` 实测），即 clone 后一定还会带过去的硬编码。config.json、.mcp.json、data/、.venv 等 gitignore 内容剔除后不再列出。

### 代码级硬编码（clone 后仍在，必须修/必须知道）

| 位置 | 内容 | 影响 / 状态 |
|---|---|---|
| `packages/mcp/server.py:23,37` | `PAN_API_URL` 默认 `http://127.0.0.1:8768` | ✅ 2026-08-19 已统一：main.py、core/config.py、qq/plugin.py、remote/main.py 默认值全部改为 8768，clone 后零配置时主服务与 MCP 同端口，不再失联 |
| `packages/mcp/monitor_workers.py:22,42` | `_DEFAULT_WS = "ws://127.0.0.1:8768/ws/agent"` | ✅ 默认已是 8768，与统一后主服务一致 |
| `packages/web/package.json:13` | `"gen:api": "openapi-typescript http://localhost:8768/api/openapi.json ..."` | ✅ 2026-08-19 已改为 8768（dev 期） |
| `packages/web/vite.config.ts:20,23,27` | dev `port: 5173`；proxy `http://localhost:8768`、`ws://localhost:8768` | ✅ 2026-08-19 已改为 8768（dev 期） |
| `packages/core/memory/embedder.py:43` | `OLLAMA_BASE_URL = "http://127.0.0.1:11434"` | 启用 memory 且走 local 模型时依赖本机 Ollama（默认值，可 env 覆盖） |
| `packages/core/memory/embedder.py:406,414` | `HF_ENDPOINT=https://hf-mirror.com`；`alt="D:/cache/huggingface"` | 国内镜像偏好 + D 盘缓存偏好（仅 C 盘且 D 盘存在时生效，可用 `HF_HOME` 覆盖，非阻塞） |
| `packages/web/index.html`、`mobile.html`、`src/index.css` | jsdelivr（katex/marked/highlight）+ Google Fonts CDN | 离线/内网机器前端样式与渲染失效 |
| `scripts/start_cf.ps1:36` | 兜底 `$env:USERPROFILE\.cloudflared\config-test.yml` | 用环境变量，随目标机用户名自适应；依赖 cloudflared 已安装并已建 tunnel |
| `scripts/stop_pan.bat` | ~~`taskkill /IM python.exe /T /F` 兜底~~ | ✅ 只杀本 checkout PID 文件记录且经身份、命令行、端口校验的进程；cloudflared 无记录时不做扫描或停止，**不再全局杀 python.exe/cloudflared** |

### 用户目录状态依赖（clone 后运行 OK，但历史数据不迁移）

| 位置 | 内容 | 影响 |
|---|---|---|
| `packages/core/adapters/cbc/sessions.py:23/57/88/133/489/526/554` | `~/.codebuddy/projects` + 绝对路径脱敏目录名（`d-project-pan`） | 新机项目路径不同 → 脱敏名不同 → 旧 transcript 失联，fork/resume 无父会话 |
| `packages/core/adapters/kimi/sessions.py:18-20,87-92` | `~/.kimi-code`，索引按绝对路径比对 workDir | 同上 |
| `packages/mcp/monitor_workers.py:96-97` | `Path.home()/".codebuddy"/"projects"` | 同上 |

### 测试 / 调试 / 文档（不阻塞运行）

| 位置 | 内容 | 影响 |
|---|---|---|
| `tests/test_kimi_adapter.py:19` | `KIMI_TEST_WORKDIR = "C:/Users/<用户名>/AppData/Local/Temp/kimi-test"` | 在其他用户名下跑测试会失败（夹具路径不存在） |
| `scripts/repro_mcp_override.py:113/116/119` | `D:\project\Pan` | 调试脚本，换机需改路径 |
| `docs/archive/cbc-mcp-experiments/*.py` | `<实验目录>/pan-stream-mcp` 等 | 实验脚本，非运行路径 |
| `packages/core/character.py:8`、`manifest_loader.py:11,123`、`cbc/sessions.py:21,172,274` | docstring 里的 `<仓库外>/RuleWhisper/...`、`D:\` 示例 | 纯文档/示例；其中 `sessions.py:274` 是通用盘符剥离逻辑，任何盘符可用，非硬编码 |

### CWD 依赖（clone 后同样存在）

| 位置 | 内容 | 说明 |
|---|---|---|
| `packages/core/manifest_loader.py` | 相对 plugin_manifests 解析 | ✅ **2026-08-22 核对已修复**：改为 rebase 到 `REPO_ROOT`（`Path(__file__).resolve().parent.parent.parent`），注释「消除 CWD 依赖」，启动方式变化不再影响 manifest 加载 |
| `packages/web/server.py`（约 L691/1838/1953/2095/2264，2026-08-27 复核） | cbc/kimi import、spawn 等默认 `Path.cwd()` | ⚠️ **仍有 CWD 依赖**（P0 sessions provider 重构后行号已变，未传 cwd 时兜底取进程工作目录），是否改为仓库根推导待决策 |
| `packages/core/adapters/cbc/worker.py:92-96` | 注释警告 CWD≠仓库根 | 已知坑 |

### git clone 后必须重做的本机准备（gitignore 剔除项）

1. **config.json** — 复制 `config.example.json` 重建（模板本身无硬编码：`remote.config_path` 默认空、`plugin_manifests` 默认仅 `["manifest.json"]`）；
2. **.mcp.json** — 重建（pan/rulewhisper 的 command/cwd），否则 MCP 不可用；
3. **.venv** — 按 requirements.txt 重建；
4. **React 前端** — `packages/web/` 需执行 `pnpm build` 生成 gitignore 的 `dist/`；
5. **ai_coc** — 仓库外依赖项目需另存到同级目录（若要用 rulewhisper 插件 / cbc-mcp 联调）；
6. **tunnel 凭据** — named tunnel 需迁移 `.cloudflared` 目录，否则改 `remote.quick_tunnel: true`。

### B. 端口硬编码 / 默认值不一致（✅ 2026-08-19 已统一为 8768）

| 位置 | 内容 | 状态 |
|---|---|---|
| `packages/mcp/server.py:37` | `PAN_API_URL` 默认 `http://127.0.0.1:8768` | ✅ 已是 8768 |
| `packages/mcp/server.py:665` | `--port default=9740` | MCP SSE/HTTP transport 端口（独立于主服务端口，保留） |
| `packages/mcp/monitor_workers.py:42` | `_DEFAULT_WS = "ws://127.0.0.1:8768/ws/agent"` | ✅ 已是 8768 |
| `data/scripts/monitor_workers.py:31` | 同上（data/ 下副本，gitignore） | ✅ 已是 8768 |
| `packages/web/vite.config.ts:20/23/27` | dev `port: 5173`；proxy target `http://localhost:8768`、`ws://localhost:8768` | ✅ 已改 |
| `packages/web/package.json:13` | `"gen:api": "openapi-typescript http://localhost:8768/api/openapi.json ..."` | ✅ 已改 |
| `scripts/start_cf.ps1:16,29` | 注释 + 端口回退 `8768` | ✅ 已改 |
| `packages/core/config.py:58`、`main.py:24`、`qq/plugin.py:32,34`、`remote/main.py:61`、`config.example.json:4` | 各默认值 `8768` | ✅ 已从 8767 统一为 8768 |
| `packages/core/memory/embedder.py:43` | `OLLAMA_BASE_URL = "http://127.0.0.1:11434"` | 启用 memory 时依赖本机 Ollama（保留） |

> 2026-08-19 处理：以 main 分支约定端口 **8768** 为标准，全仓代码默认值统一。`mcp/server.py`、`monitor_workers.py` 本已是 8768；`config.py`、`main.py`、`qq/plugin.py`、`remote/main.py`、`config.example.json`、`vite.config.ts`、`package.json`、`start_cf.ps1` 中的 8767 全部改为 8768。克隆后零配置启动，主服务与 MCP/monitor 端口一致。

### C. 用户目录 transcript 依赖（换机失联，非硬编码但需手工迁移）

| 位置 | 内容 | 影响 |
|---|---|---|
| `packages/core/adapters/cbc/sessions.py:23/57/88/133/489/526/554` | `~/.codebuddy/projects` + `sanitize_project_dir_name()`（绝对路径脱敏成目录名，如 `d-project-pan`） | **换机路径不同 → 脱敏目录名不同 → 旧 transcript 全部失联**，fork/resume 找不到父会话 |
| `packages/core/adapters/cbc/adapter.py:401` | `~/.codebuddy/projects` | 同上 |
| `packages/mcp/monitor_workers.py:96-97` | `Path.home()/".codebuddy"/"projects"` | 同上 |
| `packages/core/adapters/kimi/sessions.py:18-20` | `~/.kimi-code/`（workspaces.json / session_index.jsonl） | Kimi 索引按绝对路径比对 workDir（`_same_path`，L87-92），换路径全部失配 |
| `packages/core/adapters/claude/sessions.py`（2026-08-27 补录） | `~/.claude/projects/<encoded-cwd>/<sid>.jsonl` | 同 cbc：路径编码目录名随项目路径变化，换机失联 |
| `packages/core/adapters/codex/sessions.py`（2026-08-27 补录） | `~/.codex/`（`state_5.sqlite` threads 表含 cwd/rollout_path + `sessions/<日期>/rollout-*.jsonl`） | SQLite 记录绝对 cwd 与 rollout 路径，换机路径不同需核对 |

> 迁移建议：把 `~/.codebuddy/projects`、`~/.kimi-code`、`~/.claude/projects`、`~/.codex` 整体拷到新机同名位置，并**保持项目路径一致**（或迁移后手工改脱敏目录名），历史会话才能续上。

### D. CWD 依赖（manifest / 会话解析按进程工作目录）

| 位置 | 内容 | 说明 |
|---|---|---|
| `packages/core/manifest_loader.py:119-136` | `Path(raw_path).exists()` 按**进程 CWD** 解析 plugin_manifests | `config.json` 里的 `"manifest.json"`、`"packages/mcp/manifest.json"` 都依赖 CWD=仓库根；启动方式变了（如从别处 `python main.py`）即失效 |
| `packages/web/server.py:1371` | cbc import 默认 `str(Path.cwd())` | 同上 |
| `packages/core/adapters/cbc/worker.py:92-96` | 注释明确警告 CWD≠仓库根的坑 | 已知坑 |

### E. 次要项

- **CDN 硬编码**：`packages/web/index.html`、`mobile.html` 用 jsdelivr 加载 katex/marked/highlight，离线/内网机器前端渲染失败（建议 vendor 本地化）。
- **`.mcp.json` 里 ai_coc 依赖**：rulewhisper MCP 依赖 `<仓库外>/ai_coc` 整个项目，跨设备需一并拷贝。
- **tunnel 凭据**：named tunnel 的 cloudflared 凭据（config.yml 所在目录的 cert/token）在目标机不存在，需迁移凭据或改 quick_tunnel。
- ✅ **stop_pan.bat 误杀**：只杀本 checkout PID 文件记录且经身份、命令行、端口校验的进程；cloudflared 无记录时不扫描、不停止。

---

## 三、换机安全项（已确认 OK，无需处理）

- 仓库内数据全部相对仓库根：`data/sessions`（`packages/core/session.py:16`）、`data/characters`、`data/logs`（`logging_setup.py:34`）、`data/workdirs`、`data/mcp-configs`
- PID 文件在 `data/` 下（`start_pan.bat:13/32-33`）
- 临时文件用系统 TEMP（`start_cf.ps1:44`、tests 用 `tempfile.mkdtemp`）
- 启动脚本用 `%~dp0` / `$PSScriptRoot` 相对定位（start_pan.bat、start_main.ps1、start_cf.ps1）
- `packages/mcp/manifest.json:42,44` 用 `${PLUGIN_DIR}` 占位符，可移植
- `packages/web/server.py` 无硬编码 host/URL，WS 用相对路由，host 来自 `main.py:19` `PAN_HOST`

---

## 四、迁移建议（按优先级）

1. ✅ **统一端口默认值**（2026-08-19 已完成）：全仓以 8768 为标准。`config.py`、`main.py`、`qq/plugin.py`、`remote/main.py`、`config.example.json`、`vite.config.ts`、`package.json`、`start_cf.ps1` 的 8767 全部改为 8768；`mcp/server.py`、`monitor_workers.py` 保持 8768。
2. **config.json 去机器化**：`remote.config_path` 改为可空 + 用 `PAN_CF_CONFIG` / `%USERPROFILE%` 兜底（start_cf.ps1 已支持，只需删掉 config 里的写死值）；`plugin_manifests` 移除 `../ai_coc/...` 或改为环境变量占位。
3. **.mcp.json / data/mcp-configs 相对化**：用仓库根推导命令与 cwd（或 `${PAN_ROOT}` 占位符，参考 manifest 已有做法），避免绝对路径。
4. ✅ **CWD 依赖修复**：`manifest_loader.py` 把相对 plugin_manifests rebase 到仓库根，不依赖 CWD（2026-08-22 核对已修复，`REPO_ROOT` 推导）；`server.py` 的 `Path.cwd()` 兜底（import/spawn）待处理。
5. **新建 config.example.json 模板**：既然 config.json/.mcp.json gitignored，提供一个可直接复制填写的模板文件（注意 config.example.json 目前的 `remote.config_path` 默认已是空，但 `plugin_manifests` 默认只有 `manifest.json`，需把 ai_coc 项移除或注明）。
6. **历史会话迁移**：拷贝 `~/.codebuddy/projects`、`~/.kimi-code`，并保持目标机项目路径与旧机一致（或改脱敏目录名）。
7. **tunnel 凭据**：整体拷贝迁移时带上 `.cloudflared` 目录；否则切 `quick_tunnel: true`。

---

## 五、附：完整命中文件清单

```
# ===== 已修复（2026-08-19 / 2026-08-22 跟进） =====
main.py:24 / packages/core/config.py:58 / qq/plugin.py:32,34 / remote/main.py:61 / config.example.json:4   # 默认端口 8767 → 8768
packages/web/vite.config.ts:20,23,27   # proxy 8767 → 8768
packages/web/package.json:13           # gen:api 8767 → 8768
scripts/start_cf.ps1:16,29             # 端口回退 8767 → 8768
scripts/stop_pan.bat                   # 重写：精确杀，不再全局 taskkill /IM
scripts/start_main.ps1 / start_pan.bat / stop_pan.bat   # 2026-08-22 进一步重写：直接 venv python + 精确树杀 + 命令行 fallback（27f4f42）
packages/core/manifest_loader.py       # 2026-08-22 核对：plugin_manifests rebase 到 REPO_ROOT，消除 CWD 依赖

# ===== 仍待处理 =====
config.json:50,64                      # 用户名路径 + 仓库外插件（gitignored）
.mcp.json:5,10,14,19                   # 仓库外 venv + 绝对 cwd（gitignored）
data/mcp-configs/ses_*.mcp.json        # 运行时生成的会话级 MCP 配置（4 个）
scripts/start_cf.ps1:36                # USERPROFILE 兜底（可接受）
packages/mcp/server.py:37,665          # PAN_API_URL 默认 8768 / transport 端口 9740
packages/mcp/monitor_workers.py:42 # WS 默认 8768
packages/core/memory/embedder.py:43,406,414 # Ollama 11434 / HF 镜像+D盘缓存
packages/web/index.html/mobile.html/src/index.css # CDN 依赖
packages/web/server.py (多处 Path.cwd()) # import/spawn 默认 CWD 兜底（manifest_loader 已修，此处剩余）
packages/core/adapters/cbc/sessions.py  # ~/.codebuddy/projects 脱敏路径
packages/core/adapters/kimi/sessions.py  # ~/.kimi-code 绝对路径索引
tests/test_kimi_adapter.py:19          # 用户名测试夹具
scripts/repro_mcp_override.py:113,116,119 # 调试脚本绝对路径
```
