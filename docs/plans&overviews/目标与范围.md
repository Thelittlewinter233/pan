# 目标与范围

> 讨论顺序第 1 步：方向和边界。

---

## 1. 愿景

CLIConductor 是一个让用户**全方位、可远程地管理和对话本地 Agent CLI** 的工具平台。通过 Meta-Agent 实现多 Worker 的高级别调度，让 Agent 擅于闯入各个场景（开发、社交、日程、自动化……）。

**Phase 2 目标**：Pan 不只为自家 Worker 服务，还能作为"工具底座"被外部领域项目（首例：[RuleWhisper](https://github.com/AblazeGHR/RuleWhisper)）接入——外部项目提供 HTTP API / MCP Server，Pan 的 QQ Bot 和 Worker 承担消息入口和推理调度。

| 借鉴项目 | 借鉴点 |
|----------|--------|
| OpenClaw / Claw | 本地化 + 路由方案；AI 闯入本地管理与社交平台 |
| Tutti | 跨 CLI 工作；统一界面和 Adapter 设计 |

## 2. 边界

| 做 | 不做 |
|----|------|
| CLI Agent 调度器 | 不自己实现 AI Agent |
| 进程管理 + 消息路由 + 通道适配 | 不管理 Worker 的工作目录内容（git、文件编辑） |
| Session 可检索（摘要/标签/搜索） | 不做通用知识库 |
| MCP Server 配置透传（工具层注入） | 不实现 MCP 协议本身，不替换 Worker 通信协议 |

## 3. 核心场景（按优先级）

| 优先级 | 场景 |
|--------|------|
| P0 | Web Dashboard 远程管理、Meta-Agent 调度、QQ Bot 通道 |
| P1 | 多 CLI 适配（已落地：cbc / kimi / opencode / claude / codex 共 5 个）、Worker 快速包装为专用 Agent、远程访问 |
| P2 | 自动化流水线（定时/事件驱动） |
| P2 | 外部项目接入（通用 Manifest Loader + QQ 命令路由），首例：RuleWhisper |

## 4. 定位结论

| 决策 | 结论 |
|------|------|
| 与 CLI 工具关系 | 多 CLI 支持，Adapter 模式是核心设计 |
| QQ Bridge | 一等通道，与 Dashboard 同等重要 |
| 多用户 | 暂不，但架构预留扩展点 |
| Session 数据模型 | cbc 专有字段移入 `adapter_config`，Session 只保留通用字段 |
| 通道可见性 | 支持隔离能力。最高权限用户拥有一切数据的可视权和交互权。受限通道（如对外 QQBot）可限制其 Worker 权限（不可 spawn、不可创建 Session、不可访问工作目录外） |
| 远程认证 | 需要认证 + 来源辨析（如按 QQ 号区分用户 vs 外人） |
| 事件协议版本化 | 留到模块设计阶段定义 |

## 5. 模块拆分

### 5.1 原则

1. 每个模块独立开发、独立测试、独立部署
2. 模块间通过 HTTP/WS 接口通信，不 import 彼此内部实现
3. Core 最小化：只做进程管理 + 消息路由
4. 通道是插件

### 5.2 当前架构图（Phase 2 现状）

```
                     ┌──────────────────────┐
                     │    通道层 (Channels)   │
                     │ Dashboard / QQ Bot    │
                     │ Meta-Agent(MCP) / Remote│
                     └──────────┬───────────┘
                                │ HTTP / WebSocket / MCP
                     ┌──────────▼───────────┐
                     │       核心层 (Core)    │
                     │ Session / Worker      │
                     │ Character / Memory    │
                     │ Event Router          │
                     │ Adapter Registry      │
                     └──────────┬───────────┘
                                │ Protocol
               ┌────────────────┼────────────────┐
               │                │                │
     ┌────────▼───────┐ ┌──────▼──────┐ ┌───────▼──────┐
     │  Adapters      │ │  Memory     │ │  SDK         │
     │  cbc/kimi/opencode/claude/codex │ │  已落地      │ │  待启动      │
     └────────────────┘ └─────────────┘ └──────────────┘
```

### 5.3 模块职责

| 模块 | 职责 | 对 Core 的依赖 | Phase 2 现状 |
|------|------|---------------|-------------|
| **Core** | 进程生命周期、Session 持久化（原始数据）、事件路由、Adapter 注册、Worker Profile 管理 | — | ✅ 已清洁，API 稳定 |
| **Memory** | Session 摘要/标签/搜索、跨 Session 知识沉淀、用户偏好记忆 | Core WS 事件流 | ✅ 已落地（`core/memory/`，feature/memory 已合入 main） |
| **Adapters** | 每种 CLI 一个 adapter（启动参数、事件解析、消息编码）。通过 manifest 注入 MCP Server 配置到 spawn 参数 | Core 协议 | ✅ 5 个已实现（cbc / kimi / opencode / claude / codex），P0 共享 MCP helper + sessions provider 化，P1 执行模式显式化；MCP 注入因 CLI 而异（cbc/claude `--mcp-config`、kimi KIMI_CODE_HOME、opencode 项目级 opencode.json、codex `-c` 内联），见 `docs/design/adapter-architecture.md` §8 |
| **WebChannel** | HTTP 服务 + Dashboard 前端 + Meta-Agent WS | Core HTTP/WS | ✅ Vite+React 前端已迁移，支持 coexist/react/legacy 模式 |
| **QQChannel** | QQ Bot，将 QQ 消息转为 Core 任务。已支持 user/group 两级 Session 绑定（`_ensure_session` 按 scope 区分）；确定性前缀命令路由（manifest command_routes，`_match_command_route` 直发 HTTP API 不走 LLM）；`mirror`/`selective` 双模式；pan-qq MCP server（`qq_send_message`/`qq_read_inbox`/`qq_bind` 等） | Core HTTP/WS | ✅ 已抽离，群级绑定 + 前缀路由 + selective 均实现 |
| **Remote** | 远程隧道 + 认证 + 来源辨析 | 通道层 | ✅ Cloudflare Tunnel 已实现 |
| **SDK** | Worker 快速包装为专用 Agent | Core HTTP/WS | ⏳ 待启动 |
| **MCP**（Meta-Agent） | 将 Session/Worker 管理封装为标准 MCP 工具，供 CodeBuddy 等 Meta-Agent 直接调用 | Core HTTP/WS | ✅ `packages/mcp/` 已实现 |

**Core 与 Memory 的分界**：Core 只存原始数据（history 数组），不索引不加工。Memory 监听 Core 事件流，生成摘要/标签/知识，暴露自己的 API。

### 5.4 模块通信

所有模块通过 Core 暴露的 HTTP/WS 协议通信，不 import Core 内部实现。每个通道是 Core 的一个"客户端"。

```
# Session 管理
GET    /api/sessions                   → 列举所有 Session
POST   /api/sessions                   → 创建 Session
GET    /api/sessions/{id}              → 获取 Session 详情
GET    /api/sessions/{id}/history      → 获取历史消息
PATCH  /api/sessions/{id}              → 更新 Session
POST   /api/sessions/{id}/rename       → 重命名
POST   /api/sessions/{id}/branch       → 分支
DELETE /api/sessions/{id}              → 删除
POST   /api/sessions/batch-delete      → 批量删除

# Worker 管理
POST   /api/spawn                      → 启动 Worker
POST   /api/task                       → 发任务
POST   /api/kill/{worker_id}           → 停止 Worker
GET    /api/list                        → 列举活跃 Worker
POST   /api/worker/{id}/restart        → 重启
POST   /api/worker/{id}/settings       → 配置
POST   /api/worker/{id}/rename         → 重命名
POST   /api/worker/{id}/branch         → 分支
POST   /api/worker/{id}/interrupt      → 中断
POST   /api/worker/{id}/takeover       → 接管终端

# Adapter / 模型
GET    /api/adapters                   → 列举 Adapter
GET    /api/models                     → 模型列表
GET    /api/adapter/config             → Adapter 配置

# CBC 导入
GET    /api/cbc/projects
GET    /api/cbc/sessions
GET    /api/cbc/browse
POST   /api/cbc/sessions/import

# Kimi 导入
GET    /api/kimi/workspaces
GET    /api/kimi/sessions
POST   /api/kimi/sessions/import

# OpenCode 导入
GET    /api/opencode/sessions
POST   /api/opencode/sessions/import

# 通用 Adapter 导入（2026-08 新增，claude/codex 等走此端点）
GET    /api/adapters/{adapter}/sessions
POST   /api/adapters/{adapter}/sessions/import

# 设置 / Manifest / 模板（2026-08 新增）
GET/PUT /api/settings/ui             → 全局显示设置读写
GET    /api/session-templates        → Session 模板列表（manifest）
GET    /api/mcp/servers              → Manifest 中可选 MCP Server
POST   /api/manifest/reload          → 强制热重载 manifest
GET    /api/manifest/command-routes  → 命令路由列表
GET    /api/worker/{id}/takeover-command → 生成 takeover 命令

# WebSocket
WS   /ws                               → 事件流（广播）
WS   /ws/agent                         → Meta-Agent 专用

# 编排 / 订阅
POST   /api/report-subscribe|unsubscribe → 订阅制报告
POST   /api/claim|unclaim                → 建立 / 解除 managed 关系

# QQ 绑定（2026-08-22 新增）
POST   /api/qq/subscribe|unsubscribe     → Pan session 订阅 / 退订 QQ 会话 inbox 提醒
POST   /api/qq/notify                    → QQ 插件上报 inbox 更新（推 @@@@by qq 提醒）
GET    /api/qq/contacts                  → 最近 QQ 联系人/群

# Character / Memory（feature/memory 合入后新增）
GET    /api/characters/profiles        → 列出 session_template（旧称 Profile）
POST   /api/characters                 → 创建 Character
GET    /api/characters                 → 列出 Character
GET/DELETE /api/characters/{id}        → 详情/删除
POST   /api/memory/index|inject        → 索引 / 注入记忆
GET    /api/memory/search|stats        → 检索 / 统计

# 文件系统（session workdir 内）
GET    /api/fs/list|read               → 列表 / 读取
POST   /api/fs/write|rename|delete     → 写 / 重命名（目标存在时原子失败，不覆盖）/ 删除
```

> **接口表为 2026-08 中旬的阶段快照**，仅作模块划分示意；此后新增/改动的端点（如
> `POST /api/sessions/order` 拖拽排序、`/api/sessions/{id}/queue` 会话队列 CRUD、
> `GET /api/sessions/{id}/managers` manager 链、`POST /api/readonly`、`/api/notify`、
> session 寻址的 worker 控制端点等）不再逐一回填本表。**现行完整 HTTP/WS 清单以
> [`docs/skills/pan/references/http-api.md`](../skills/pan/references/http-api.md) 与
> `packages/web/server.py` 为准**（README「API 概览」保留常用子集）。

### 5.5 跨平台

跨平台改造已在 Phase 2 早期完成：`worker.py` 使用 `psutil` 替代 `taskkill`，终端接管使用 `platform_opener()` 封装，CLI 路径使用 `shutil.which()` 查找。

### 5.6 外部项目接入模式

Pan 作为"工具底座"，外部领域项目（如 RuleWhisper）通过两种方式接入：

1. **HTTP API 前缀路由**：外部项目的 manifest.json 声明 `command_routes`，Pan 的 QQ Bot 插件遍历并匹配前缀，命中后直发 HTTP API，不创建 Worker。
2. **MCP Server 透传**：外部项目的 manifest.json 声明 `mcp_servers`、`session_templates` 和 `character_templates`，Pan 的通用 Manifest Loader 加载并合并到全局池。Worker spawn 时注入 `--mcp-config`。

外部项目**不 import Pan Core 内部**，仅通过 HTTP/WS 或 MCP 协议与 Pan 通信。

## 6. 迁移路径：单仓渐进 — 当前状态

```
Phase 1 (已完成)        Phase 2 (进行中)         Phase 3 (目标)
packages/core/          packages/core/ (稳定)    packages/core/ (稳定)
├── API 清洁化 ✅       packages/adapters/       packages/adapters/*
├── 跨平台适配 ✅        ├── cbc ✅               packages/memory/（已在 core 内）
└── cbc 字段独立 ✅      ├── kimi ✅
                        ├── opencode ✅
                        ├── claude ✅
                        └── codex ✅              packages/qq/ (稳定)
                        core/memory/ ✅          packages/web/ (稳定)
qq-bridge/ ──→           ├── 已落地（feature/memory） packages/remote/ (稳定)
packages/qq/ ✅         packages/qq/ ✅           packages/mcp/ (稳定)
packages/web/ ✅         ├── plugin.py ✅         packages/sdk/
├── FastAPI + WS ✅      └── bot.py ✅
├── Vite+React ✅       packages/web/ ✅
├── Legacy TS 并存 ✅     ├── server.py ✅
└── 三前端模式可选 ✅      ├── Vite+React SPA ✅
                        ├── Legacy TS 共存 ✅
                        └── 三前端模式可选 ✅
                        packages/remote/ ✅
                        packages/mcp/ ✅
                         ↑ 各通道已抽离；Memory 已落地；SDK 待启动
```

| Phase | 内容 | 关键交付 | 状态 |
|-------|------|---------|:----:|
| 1 | 清洁 Core：内部重构，外部只走 HTTP/WS；移 cbc 字段到 `adapter_config` | 稳定的 Core API 契约 | ✅ 完成 |
| 2 | 逐个抽离通道：QQ/Web/Remote/MCP 已抽离，Memory 已落地，SDK 待启动。RuleWhisper 联动（manifest/profile/character）已完成 | 各模块独立目录 | 🟡 进行中 |
| 3 | 所有模块独立迭代 | 架构目标达成 | ⏳ |

## 7. 待后续阶段讨论

- [x] 前端 Dashboard：纯 TS vs 框架（React/Vue）？→ 已选 React 19 + Vite 6，迁移完成 ✅
- [x] QQChannel：NoneBot2 vs 更轻量方案？→ 已选 NoneBot2，已抽离为独立模块 ✅
- [x] QQ Bot 确定性命令路由 → 已有方案：前缀路由 + Webhook 配置（`RuleWhisper联动与框架优化建议.md` 第三章节）
- [x] 群级 Session 绑定 → 已有方案：`_ensure_session` 按 `scope: "user" | "group"` 区分（同上文档第四章节）
- [x] Worker Profile / Persona → 已有方案：`config.json` 增 `profiles` 字段（同上文档第五章节）
- [x] MCP 透传机制 → 已有方案：adapter 层配置转发，无需理解协议语义（同上文档第二章节）
- [x] Remote 远程访问方案 → 已实现 Cloudflare Tunnel ✅
- [x] 跨平台适配 → 已使用 `psutil` 替代 `taskkill`，CLI 路径配置化 ✅
- [x] Memory 第一版实现方案 → 已实现（`core/memory/`，feature/memory 已合入 main，88 tests）✅
- [x] Worker 状态机补全（spawning / zombie / queued）→ zombie/queued 已实现；`spawning` 已移除（stream 模式无 init 事件，spawn 即 idle，见 `Meta-Agent实现记录.md`）✅
- [ ] Packaging SDK 具体形态（代码生成 vs 配置模板）→ 触发条件：第二个外部项目接入
- [x] QQ 前缀命令路由（manifest command_routes 直发外部 HTTP API）→ 已实现（2026-08-22，plugin.py `_match_command_route`）✅
- [ ] Adapter 与 Core 的耦合程度（同进程 vs 独立客户端）
- [ ] 事件协议版本化和 stable API 清单
- [ ] 外部项目接入规范（HTTP API 契约、MCP Server 注册约定）
- [x] 轮询改 WebSocket 推流（QQ Bot 插件侧）→ 已实现（2026-08-17，P5：plugin.py WS 推流 + 5s 重连 + `_consumed_seq` 去重）✅
