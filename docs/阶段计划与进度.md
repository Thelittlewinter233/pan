# 阶段计划与进度

> 合并自「下一阶段优化计划.md」+「Phase2-收尾功能计划.md」（2026-08-16 合并）。
> **2026-08-16 重写**：核对代码现状——原 P0/P1/P3/P6 均已实现，剩余 P2/P4/P5。
> **2026-08-17 更新**：P4/P5/F1/F2/L1/L2/L3/L6 已完成，仅剩 P2（SDK）。
> **2026-08-23 更新**：新增 1.4 本批变更（QQ 绑定 / worker 串行 / 进程管理 / manage-postbox / React 实时更新）。
> **2026-08-27 更新**：新增 1.5 本批变更（5 CLI adapter 全就位、session_handoff、history 增量持久化、默认前端切 React 等）；React 重构/消息队列/import 设计三份已完成方案归档至 `docs/archive/`。
> **2026-09-03 更新**：新增 1.6 本批变更（统一服务端消息队列第一版、会话自定义排序/拖拽真实 API、Manage「Managed by」镜像父级控制等）。
> 状态：路线图/进度追踪

---

## 一、当前状态盘点

### 1.1 Phase 2 主要交付（已落地）

| 模块 | 状态 | 说明 |
|------|------|------|
| Core 清洁化 | ✅ | `packages/core/` API 稳定 |
| QQ Channel | ✅ | `packages/qq/`，群级 session 绑定 + 前缀命令路由（P0 已实现）|
| Web Channel | ✅ | React SPA 唯一前端；根路径跳转 `/react/` |
| Remote Channel | ✅ | Cloudflare Tunnel |
| MCP（Meta-Agent） | ✅ | `packages/mcp/` 标准 MCP 工具 |
| Memory | ✅ | `packages/core/memory/` SQLite+FTS5+embedding + session transcript 索引（P3 已实现）|
| Manifest Loader | ✅ | `packages/core/manifest_loader.py` profiles/mcp_servers/command_routes |
| Adapter（5 个 CLI） | ✅ | cbc / kimi / opencode / claude / codex 全就位；P0 共享 MCP helper + sessions provider 化；P1 执行模式显式化（`execution_modes` + oneshot）。cbc 走 `--mcp-config`，kimi 走 `KIMI_CODE_HOME` 隔离（方案 C），opencode 走项目级 opencode.json，codex 走 `-c` 内联注入。见 `docs/design/adapter-architecture.md` 与 `docs/references/cli-adapter-special-behaviors.md` |
| Character/Profile | ✅ | profile → character → memory（已完成：拆 session_template + character_template，见 `docs/archive/Character概念分层重构立项.md`）|
| Worker 状态机 | ✅ | zombie/queued/held 三态 + watchdog 回收（P1 已实现）|
| React 编辑器 | ✅ | `src/components/editor/CodeEditor.tsx`（P6 已实现）|

### 1.2 MCP 链路进度

- [x] Pan → cbc → RuleWhisper 完整链路（07-30 首通走 ToolSearch→DeferExecuteTool；**08-16 起 `--mcp-config` 路径工具直接可用**，见踩坑记录 #9）
- [x] 双模式 worker（stream / one-shot MCP）
- [x] **stream 长驻 + MCP**（`output_mode="stream"`，cbc 2.137.0 起 stream-json 与 `--mcp-config` 兼容，2026-08-16 实测）
- [x] `--resume` 重新启用（commit `80d5f7c`）— 后续消息 ~35s 稳定
- [x] 首条消息冷启动解决（~90s → ~16s）— `coc-keeper-coldstart` profile
- [x] 错误处理：MCP 超时用户可见、非零退出码携带 stderr、16MB 输出上限

### 1.3 React 前端

React SPA 已合入 main，根路径稳定 307 重定向到 `/react/`。Vanilla 前端已于 2026-09-11 退役，详见 `docs/archive/vanilla-frontend-retirement.md`。

### 1.4 本批变更（2026-08-22 ~ 08-23）

> 对应 commit：`27f4f42`（QQ 绑定 / worker 串行 / 进程管理）、`ae109e4`（前端 manage/postbox）、`15889a0`（React 实时更新）、`5d706db`/`b76ccd1`/`d9c13b4`/`4e3d24f`（QQ selective 模式 + 独立 MCP + 配置化）、`634a757`（SMA template）、`c0ed9c6`（README 通用 Agent CLI）。

| 模块 | 状态 | 说明 |
|------|------|------|
| **QQ session 绑定 + inbox 推送** | ✅ | Session 新增 `qq_subscriptions` 字段；`packages/qq/mcp.py`（pan-qq MCP）新增 `qq_bind`/`qq_unbind`；Pan Core 新增 `POST /api/qq/subscribe|unsubscribe|notify|contacts`；QQ 插件 `_append_inbox` 落盘后通知 Pan Core，订阅者经 `queue_pending` + report_signal 收到 `@@@@by qq` 提醒（镜像 report_subscribe 订阅制） |
| **QQ selective 模式** | ✅ | `config.json -> qq.mode`（或 `PAN_QQ_MODE`）：`mirror`（默认，全量镜像自动回复）/ `selective`（消息只进 inbox + history，不自动回复，由 meta-agent 经 pan-qq MCP `qq_read_inbox`/`qq_send_message` 决策）。QQ 前缀命令路由（manifest `command_routes`）与 group→game_id 解析亦已实现（`_match_command_route` / `_resolve_game_id` / `_sync_session_game_id`） |
| **worker stream 串行修复** | ✅ | `_consumer_stream` 写 stdin 后等待 `_task_done` asyncio.Event（result 完成 / EOF 时 set），修复连续快速派发导致的多消息同时在 cbc 管道飞行、result 与 seq/taskId 错位、history 重复 |
| **进程管理** | ✅ | `main.py` 按 `config.qq.enabled` 统一 spawn/终止 QQ bot（写 `data/qq_bot.pid`）；重写 `scripts/start_main.ps1`（直接 venv python）、`start_pan.bat`、`stop_pan.bat`（精确树杀 + 命令行 fallback，不再全局杀 python.exe） |
| **前端 manage/postbox** | ✅ | React session 卡片新增 Manage（`/api/claim`/`/api/unclaim` 管理 managed 关系）与 Postbox（`/api/qq/subscribe`/`/api/qq/unsubscribe` 管理 QQ 订阅）弹窗，带搜索筛选；后端补 `/api/unclaim`、`_session_to_api` 补 `qqSubscriptions` |
| **React 实时更新修复** | ✅ | `useWebSocket.ts` worker.result 后 `fetchSession` + `updateFromServer` 刷新列表；补订阅 `session.created`/`session.deleted`/`sessions.deleted` 事件 |
| **SMA session template** | ✅ | `manifest.json` 新增 `SMA`（Super Meta Agent）session template：全权限（can_claim_unmanaged + auto_claim_created）+ `pan` / `pan-qq` 双 MCP |

---

### 1.5 本批变更（2026-08-23 ~ 08-27）

> 关键 commit：`b46eb11`（claude adapter）、`c8f6f96`（codex adapter）、`3b13c5b`（opencode adapter）、`2641dc0`/`f3ed2d3`（adapter P0 重构）、`275f7e2`（P1 执行模式显式化）、`eacd3b3`（kimi MCP 方案 C）、`4fcac4b`（opencode MCP）、`75840d7`（QQ 通道插件化 + LLOneBot）、`752ec49`（main.py QQ spawn 注入 PYTHONPATH）、`f7cfff5`（session_handoff 替身交接）、`60e251b`（移除 worker_handoff）、`fe97aec`（history 增量持久化）、`7c0e691`（发送队列入队即上屏）、`d25c6e0`/`2389ec6`（默认前端切 React，旧入口已后续归档）、`eb1f223`（watchdog idle 回收修复）、`67e178e`（ModelPill 模型切换搜索框）、`c67a528`（移动端设置整页）、`2a32d86`（session 菜单视口修复）。

| 模块 | 状态 | 说明 |
|------|------|------|
| **5 个 CLI adapter 全就位** | ✅ | cbc / kimi / opencode / claude / codex；P0 共享 MCP helper + sessions provider 化（`adapters/mcp.py`、`SessionsProvider` Protocol、通用导入端点 `/api/adapters/{adapter}/sessions[/import]`）；P1 执行模式显式化（`execution_modes` / `oneshot_args` + React 设置弹窗 output mode 选择器） |
| **QQ 通道插件化 + LLOneBot** | ✅ | `QQChannel` 基类 + napcat / llonebot 两个薄子类，`config.json -> qq.channel` 切换（默认 napcat）；main.py spawn QQ bot 子进程注入 PYTHONPATH（修复 miniforge 解释器依赖） |
| **session_handoff 替身交接** | ✅ | 新增 `session_handoff` MCP 工具（孪生 session 接替）；旧 `worker_handoff` 端点/工具已彻底移除，串行依赖统一走 `worker_assign` + report_subscribe |
| **history 增量持久化** | ✅ | 根治 O(history) 全量序列化；queue 操作不随 history 规模增长（性能回归测试） |
| **发送队列入队即上屏** | ✅ | 队列消息入队即乐观上屏，修复发出后聊天窗口不显示（取代 design-message-queue 的「入队不上屏」设计） |
| **默认前端切 React** | ✅ | `/` 307 重定向 `/react/`；后续已移除旧前端入口和 frontend 配置 |
| **worker 稳定性** | ✅ | watchdog idle 回收修复（restore idle reclaim）；cbc 模型缓存 TTL 5 分钟；codex resume 保留审批 flag、fork 物化 rollout + MCP `-c` 注入修正 |
| **前端打磨** | ✅ | ModelPill 模型快速切换加搜索框（复用 ModelSelect）；移动端 Manage 设置改整页；session 菜单不越出视口；session 默认命名取最小可用序号 |

---

### 1.6 本批变更（2026-08-27 ~ 09-03）

> 关键 commit：`1d705dc`/`11c187f`（自定义排序 API + React 拖拽真实对接）、`c5543ea`（source metadata 边界校验）、`5e54ea7`/`928a233`（队列跨来源排序修复）、`11420d7`/`8b96079`（Manage「Managed by」镜像父级控制 / mobile-manage-layout 合入）、`93bbe92`（Python 依赖环境收敛跟踪）。

| 模块 | 状态 | 说明 |
|------|------|------|
| **统一服务端消息队列（第一版）** | ✅ | `Session.queue_pending` 升级为服务端权威队列：typed envelope（`type/kind/id/source/position/deliveryState/revision`）+ `queue_delivery_ledger` 幂等账本 + `queue_revision`；入队/查询/编辑/排序/删除/retry 六组端点（`/api/sessions/{id}/queue*`）；用户消息**落盘确认后才 accepted**（浏览器 WS `user_inject` 与 `POST /api/sessions/{id}/queue` 同入口）。交付语义：写成功后出队（`queued→reserved→writing→sent_to_cli`），失败/崩溃归队 + 指数 backoff，**明确接受窄重复窗口而非静默丢失**（设计：`docs/design/queue-at-most-once.md`、`docs/plans&overviews/统一服务端消息队列改动计划.md`） |
| **会话自定义排序 + 拖拽真实 API** | ✅ | Session 新增 `order` 字段 + `POST /api/sessions/order`（部分重排允许）；React SessionList 同层拖拽持久化顺序（`?mock=1` 演示模式除外），跨 manager 拖拽（拖到卡片正中）复用 `claim`/`unclaim` 建立/解除管理关系；WS 广播 `session.orderUpdated` |
| **source metadata 边界校验** | ✅ | `_request_source_metadata` 在 HTTP 边界校验 `source/sourceSessionId`（允许集合 `user/agent/system_prompt/report/meta-agent/automation`），不再强制 agent 源；只读目标拒绝外来任务返回 `readonly_session`（`c5543ea`） |
| **Manage「Managed by」镜像父级控制** | ✅ | Manage 弹窗「Managed by」区显示并可直接操作父 manager 的控制（Unmanage / 订阅开关 / Readonly），移动端整页同步（`11420d7`） |
| **Python 依赖环境收敛（跟踪）** | ⏳ | 依赖审计（2026-09-02）发现 .venv / C 盘全局 / Miniforge 同栈副本漂移；先核对锁定依赖再决定收敛（待办在 TODO） |

---

## 二、剩余待办

| 优先级 | 项 | 说明 | 触发条件 |
|--------|----|------|---------|
| ~~P4~~ | ~~Worker 间通信（管理归属 + 报告）~~ | **已完成（2026-08-17）**：role 权限、managed/managed_by、queue_pending 报告（订阅制）、全局 watchdog 自愈、MCP 隔离、默认 MCP 全部合入 | ✅ |
| P2 | **SDK 模块** | 让 Worker 包装为专用 Agent。形态未定（代码生成 vs 配置模板），**需先立项** | 有第二个外部项目（非 RuleWhisper）接入时 |
| ~~F1~~ | ~~Character 概念分层重构~~ | **已完成（2026-08-17）**：Profile→`session_template` 改名 + `character_template` 新增 + character 瘦身 + `_build_session_params` 从 template 读配置 | ✅ 归档 `docs/archive/Character概念分层重构立项.md` |
| ~~F2~~ | ~~Role 取消 + 能力字段拆解~~ | **已完成（2026-08-17）**：删 `role`，拆 `restrict_to_managed`/`can_claim_unmanaged`/`auto_claim_created`，`_check_access` 改能力字段判定 | ✅ 归档 `docs/archive/Role字段取消与能力字段拆解立项.md` |
| ~~P5~~ | ~~QQ Bot 轮询改 WebSocket 推流~~ | **已完成（2026-08-17）**：`plugin.py` 改 WS 推流（订阅 worker.result + 5s 重连 + `_consumed_seq` 去重 + 轮询 fallback）| ✅ |

### 遗留待办（2026-08-17 汇总）

| # | 待办 | 来源 | 说明 |
|---|------|------|------|
| ~~L1~~ | **A4 watchdog timeout 根治** | E1 日志发现（worker 读大文件被 300s 误杀）| **已完成（2026-08-17）**：stream running 超时改「任务运行时长」（`_task_started_at` 起算，默认 1800s），queued 保留静默超时，区分长思考 vs 卡死 |
| ~~L2~~ | **config.example.json 同步 mcp 段** | 默认 MCP 配置改动 | **已完成（2026-08-17）**：`config.example.json` 补 mcp 段（`enabled_default` 已随「MCP 单一事实源收敛」废弃，注明不再读取）|
| ~~L3~~ | **skill 维护规范：自包含验收** | 冷启动实测（D5 暴露 G1-G7）| **已完成（2026-08-17）**：SKILL.md 主源（docs/skills/pan/）补 §13「skill 维护规范」——文档/手册任务必须自包含可验证（编写与验证闭环）+ SKILL.md 双份管理说明 |
| L4 | **watchdog 分支不确定点** | B3+B4 报告 | `pending_signal` 载荷收窄（只放 item.id）留待真源迁移后与 consumer 侧同步；4.6 worker 级 watchdog 是否整体替换为全局级待评估（当前只新增全局级，未动 worker 级）|
| L5 | **MCP 隔离细节** | B5 报告 | 需确认：隔离的权限边界、claim 释放时机、与报告订阅的交互（详见 Profile 立项 B5）|
| ~~L6~~ | **报告 done/error 语义** | B1 报告 | **已完成（2026-08-17）**：done/error 都入队确认为决策保留（协调者需知道失败），`_enqueue_report` docstring 补注释说明 |

### Pan 稳定性修复（2026-08-17 e2e 发现，已合入 pan-test）

| 修复 | 根因 | 提交 |
|------|------|------|
| **`_read_stdout` 分块读取 + 大行截断** | Windows asyncio `readline()` 读大 tool result（Read 大文件单行几百KB-MB）永久挂起 → Pan 卡死 | `252c41d` |
| **replay 等待超时上限** | kill 后 resume，transcript 无 result（任务被 kill 未完成）→ replay 永不结束 → `_consumer` 死等，新消息丢失 | `7ddc3ea` |
| **broadcast 慢客户端超时** | 慢 WS 客户端（如 Edge 后台页面不消费）阻塞 `send_json` → 所有 worker 卡死 | `91d8323`（f1-char）|
| **replay 竞态** | `_consumer` 无条件重置 `_replaying`，resume replay 被记录为重复历史 | `d452805`（f1-char）|

### 整合：pan-test 审查问题处置（2026-08-21 核对，源自 `docs/pan-test-分支质量审查-2026-08-15.md`，原文档已删除）

原审查 H1/H2/M1/M2/M3/L1/L2/L4/L5 经代码核对**均已修复**（worker.py 统一 `_current_seq`、`_task_status` TTL + kill 标 error、`_result_waiters` 多槽位、MCP idle 刷新 `last_activity`、reconnect 按 `consumed_seq < latest_seq` 补发 + taskSeq 补存、`agent_subscriptions.setdefault`、发送成功后才推进 consumed_seq、config.example.json worker 段、handoff/assign 改 WS 信封）。残余待确认：

| # | 项 | 说明 |
|---|----|------|
| ~~R1~~ | **L3 idle 覆盖边缘窗口** | **已解决（2026-08-29）**：watchdog 回收前检查 `pending_signal` 与持久 `queue_pending`，有待消费工作时不回收；补回归测试 |
| ~~R2~~ | **补集成测试** | **已完成（2026-08-29）**：新增 Codex stream 序号/终态配对、WS 中途断线后新序号补发，以及超时→kill→同 taskId 重试回归覆盖 |

### 整合：质量审查整改待决策项（2026-08-21 整合自 `docs/archive/质量审查整改立项.md`，原文档已删除）

「可直接修 8 项」已全部落地（`s=None` 初始化、`_replaying` try/finally、`deepcopy(DEFAULT_CONFIG)`、`subprocess.run` 列表参数免 shell、`/api/fs/write` 5MiB 上限、useWebSocket unsubscribers 清理、草稿走 inputDrafts、`collapseAllGroups(keys)`）。剩余需决策的开放项：

| # | 项 | 说明 |
|---|----|------|
| D1 | **workdir 放行策略** | `_ALLOWED_WORKDIR_ROOTS=None` 对绝对路径直接放行，结合 `/api/fs/*` 可读写删任意目录——鉴权推迟后唯一实际安全边界，**优先决策**（A 白名单根目录 / B 绝对路径只读 / C 维持现状） |
| D2 | **manifest 信任模型** | `_parse_mcp_server` 不校验 command/args/env/cwd，manifest 可声明任意可执行文件被 cbc 子进程执行（A 视为受信记录在案 / B 容纳校验 + command 白名单） |
| D3 | **server.py 拆分** | God object（约 4800 行、约 99 个 HTTP 路由，2026-09-03 统计；早期「1911 行/40+ 路由」为旧基线），cbc/kimi 逻辑成对复制（A 按领域拆 Router / B 抽公共 helper / C service 层） |
| ~~D4~~ | ~~legacy app.ts 去留~~ | **已归档（2026-09-11）**：旧前端及其入口已删除，React 为唯一前端 |
| D5 | **enrich 协议 sync→async** | `enrich_after_result` 内 `time.sleep(0.2)` 阻塞事件循环 0.2s/次（A Protocol 改 async / B sleep 移出 / C 容忍） |
| D6 | **kimi fork 判定** | `KimiAdapter.supports_fork=True` 但 `fork_args` 返回 `[]`，`branch_worker` 判「不支持 fork」→ kimi 分支永远失败且残留孤儿目录（A 只依赖 supports_fork / B fork 后走非 fork 路径） |
| D7 | **Session 原子写 + 缓存锁** | `write_text` 非原子 + `save_async` 与事件循环并发写同一文件 + `_cache` 无锁（临时文件 + `os.replace` + `threading.Lock`） |
| D8 | **广播并发发送** | `broadcast` 对同一 WS 无锁并发 `send_json`（慢客户端超时已修 `91d8323`，连接级锁未做） |
| D10 | **load_config 缓存 vs 热重载** | `_cbc_config` 每次属性访问读盘；直接缓存会破坏「运行时编辑 config.json 立即生效」（A 缓存 / B 缓存+mtime / C 现状） |
| D12 | **API key 落库改 hash** | `embedder.py` 用 `api_key[-8:]` 作 `provider_key`（改 `sha256`，旧缓存一次性失效） |
| D11 | **ANN 升级** | 触发条件型：单 character chunk >~1 万才立项（当前数据量小） |

> 已办：D0 鉴权推迟（既定姿态，绑 loopback）、D9 QQ 串线（已被 P5 WS 推流 + `_consumed_seq` 去重实质解决）、D13 端口统一 8768（跨设备移植报告）、D14 provider 选型 ST（`docs/archive/embedding依赖与venv环境治理立项.md` 已删，结论：requirements 已补 sentence-transformers、llama-cpp-python 保留预留、默认 provider 维持 ST）。

### 跨设备移植（2026-08-19，报告见 `docs/跨设备移植报告-2026-08-19.md`）

> 已完成：端口默认值全仓统一 8768；`stop_pan.bat` 重写（不再全局杀 python.exe）；**CWD 依赖修复（manifest_loader.py 相对 plugin_manifests rebase 到 `REPO_ROOT`，不再依赖进程 CWD，2026-08-22 核对代码确认）**；`start_main.ps1` / `start_pan.bat` / `stop_pan.bat` 进一步重写（精确树杀 + 命令行 fallback，见 1.4 进程管理）。

| 优先级 | 待办 | 说明 |
|--------|------|------|
| 高 | **config.json 去机器化** | `remote.config_path` 改为可空 + `PAN_CF_CONFIG` / `%USERPROFILE%` 兜底；`plugin_manifests` 移除 `../ai_coc/...` 或改环境变量占位（gitignored，整体拷贝迁移必改） |
| 高 | **.mcp.json / data/mcp-configs 相对化** | 用仓库根推导 command/cwd（或 `${PAN_ROOT}` 占位符），避免绝对路径 |
| 中 | **CWD 依赖修复（剩余）** | `manifest_loader.py` 已完成（rebase 到 `REPO_ROOT`）；`server.py` 仍有 5 处 `Path.cwd()` 兜底（约 L691/1838/1953/2095/2264，未传 cwd 时默认进程 CWD）——2026-08-27 核对仍存在，是否需改为仓库根推导待决策 |
| 中 | **历史会话迁移** | cbc `~/.codebuddy/projects`、kimi `~/.kimi-code`、claude `~/.claude/projects`、codex `~/.codex/sessions` 整体拷贝 + 保持目标机项目路径一致（各 adapter sessions.py 按绝对路径脱敏） |
| 中 | **CDN vendor 本地化** | `packages/web/index.html`、`mobile.html` 的 jsdelivr（katex/marked/highlight）+ Google Fonts，离线/内网机器前端渲染失效 |
| 低 | **tunnel 凭据** | 整体拷贝迁移时带上 `.cloudflared`；否则改 `remote.quick_tunnel: true` |
| 低 | **测试夹具用户名** | `tests/test_kimi_adapter.py:19` `KIMI_TEST_WORKDIR` 含本机用户名；已加 `skipif`（2026-09-03 核对：目录缺失时跳过而非失败），跨机不再 red |

---

## 三、首条消息性能优化（已解决，历史）

~~通过克隆"已探索"session 历史上下文注入首次调用，跳过首条冷启动。~~

**当前方案**：`coc-keeper-coldstart` profile（commit `f9a28da`）
- system_prompt 注入权威系统信息：声明权威（禁止验证/探索）、硬性禁止 Bash/Grep/Read/Agent、需要数据直接用 MCP 工具（08-16 起直接调用，无需 ToolSearch）
- 实测：首条消息 ~90s → ~16s（5.6x）（历史：当时经 1 次 `DeferExecuteTool(game_list)`）
- `725e680` 补充：branch session 继承 character 绑定 + MCP 配置 + 跨目录查找 session JSONL

> 注：该 profile 的 prompt 断言"已连接 RuleWhisper MCP"，依赖 `config.json` 的 `plugin_manifests` 配置了 RuleWhisper 插件；未配置时会幻觉调用。

---

## 四、关联文档

- `docs/TODO.md` — **全部待办/待决策项统一汇总**（2026-08-27 起，本文档的待办以此为准）

- `docs/plans&overviews/目标与范围.md` §7 — Phase 2 待后续讨论清单
- `docs/plans&overviews/RuleWhisper联动方案.md` §6/§11 — QQ 命令路由实施细节（P0 已实现）
- `docs/archive/Profile权限字段与MetaAgent管理Session立项.md` — Worker 间通信/管理归属（P4）
- `docs/archive/react-refactor-plan.md` — React 前端路线图（已归档，迁移完成）
- `docs/cbc-mcp-踩坑记录.md` — MCP 接入过程与 stream+MCP 结论
- `docs/references/cbc-mcp-defer-机制.md` — 加载路径与 defer 机制
- `docs/跨设备移植报告-2026-08-19.md` — 跨设备移植硬编码清单与迁移建议
- `docs/archive/qq-llm-management-survey.md` — QQ 框架调研；推荐方案 A（selective + pan-qq MCP）已实现，见 1.4
- `docs/archive/design-message-queue.md` — 消息发送队列设计（已实现；「入队不上屏」已被 1.5 的入队即上屏取代）
- `docs/archive/design-import-session-mcp.md` — `session_import` MCP 工具设计（已实现）
