# Legacy full local-development entry point.
# Runtime-only installs should use minimal-requirements.txt. This file keeps
# the familiar command working by composing runtime, test, and optional
# Memory layers; QQ remains separate in packages/qq/requirements.txt.
-r dev-requirements.txt
-r memory-requirements.txt
